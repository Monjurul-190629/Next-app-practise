import React from 'react';

const page = () => {
    throw new Error('There is a huge error')
    return (
        <div>
            This is Mission page
        </div>
    );
};

export default page;